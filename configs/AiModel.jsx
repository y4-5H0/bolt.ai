import {
  GoogleGenerativeAI,
  HarmCategory,
  HarmBlockThreshold,
} from '@google/generative-ai';

const apiKey = process.env.NEXT_PUBLIC_GEMINI_API_KEY;
const genAI = new GoogleGenerativeAI(apiKey);

const model = genAI.getGenerativeModel({
  model: 'gemini-2.0-flash-exp',
});

const generationConfig = {
  temperature: 1,
  topP: 0.95,
  topK: 40,
  maxOutputTokens: 8192,
  responseMimeType: 'text/plain',
};
const CodeGenerationConfig = {
  temperature: 1,
  topP: 0.95,
  topK: 40,
  maxOutputTokens: 8192,
  responseMimeType: 'application/json',
};

export const chatSession = model.startChat({
  generationConfig,
  history: [],
});

export const GenAiCode = model.startChat({
  generationConfig: CodeGenerationConfig,
  history: [
    {
      role: "user",
      parts: [
        {text: "generate a to do app . Generate a Project in React. Create multiple components, organizing them in separate folders with filenames using the .js extension, if needed. The output should use Tailwind CSS for styling, \nwithout any third-party dependencies or libraries, except for icons from the lucide-react library, which should only be used when necessary. Available icons include: Heart, Shield, Clock, Users, Play, Home, Search, Menu, User, Settings, Mail, Bell, Calendar, Star, Upload, Download, Trash, Edit, Plus, Minus, Check, X, and ArrowRight. For example, you can import an icon as import { Heart } from \"lucide-react\" and use it in JSX as <Heart className=\"\" />.\nalso you can use date-fns for date format and react-chartjs-2 chart, graph library\n\nReturn the response in JSON format with the following schema:\n{\n  \"projectTitle\": \"\",\n  \"explanation\": \"\",\n  \"files\": {\n    \"/App.js\": {\n      \"code\": \"\"\n    },\n    ...\n  },\n  \"generatedFiles\": []\n}\n\nHere’s the reformatted and improved version of your prompt:\n\nGenerate a programming code structure for a React project using Vite. Create multiple components, organizing them in separate folders with filenames using the .js extension, if needed. The output should use Tailwind CSS for styling, without any third-party dependencies or libraries, except for icons from the lucide-react library, which should only be used when necessary. Available icons include: Heart, Shield, Clock, Users, Play, Home, Search, Menu, User, Settings, Mail, Bell, Calendar, Star, Upload, Download, Trash, Edit, Plus, Minus, Check, X, and ArrowRight. For example, you can import an icon as import { Heart } from \"lucide-react\" and use it in JSX as <Heart className=\"\" />.\n\nReturn the response in JSON format with the following schema:\n\njson\nCopy code\n{\n  \"projectTitle\": \"\",\n  \"explanation\": \"\",\n  \"files\": {\n    \"/App.js\": {\n      \"code\": \"\"\n    },\n    ...\n  },\n  \"generatedFiles\": []\n}\nEnsure the files field contains all created files, and the generatedFiles field lists all the filenames. Each file's code should be included in the code field, following this example:\nfiles:{\n  \"/App.js\": {\n    \"code\": \"import React from 'react';\\nimport './styles.css';\\nexport default function App() {\\n  return (\\n    <div className='p-4 bg-gray-100 text-center'>\\n      <h1 className='text-2xl font-bold text-blue-500'>Hello, Tailwind CSS with Sandpack!</h1>\\n      <p className='mt-2 text-gray-700'>This is a live code editor.</p>\\n    </div>\\n  );\\n}\"\n  }\n}\n  Additionally, include an explanation of the project's structure, purpose, and functionality in the explanation field. Make the response concise and clear in one paragraph.\n  - When asked then only use this package to import, here are some packages available to import and use (date-fns,react-chartjs-2,\"firebase\",\"@google/generative-ai\" ) only when it required\n  \n  - For placeholder images, please use a https://archive.org/download/placeholder-image/placeholder-image.jpg\n  -Add Emoji icons whenever needed to give good user experinence\n  - all designs I ask you to make, have them be beautiful, not cookie cutter. Make webpages that are fully featured and worthy for production.\n\n- By default, this template supports JSX syntax with Tailwind CSS classes, React hooks, and Lucide React for icons. Do not install other packages for UI themes, icons, etc unless absolutely necessary or I request them.\n\n- Use icons from lucide-react for logos.\n\n- Use stock photos from unsplash where appropriate, only valid URLs you know exist. Do not download the images, only link to them in image tags."},
      ],
    },
    {
      role: "model",
      parts: [
        {text: "```json\n{\n  \"projectTitle\": \"React Todo App with Tailwind CSS\",\n  \"explanation\": \"This project implements a simple Todo application using React and Tailwind CSS. The application is structured into components located in the '/components' folder, including a Todo list, a Todo form, and a filter bar. It features basic add, delete and complete functionality for to-do items, using local storage to persist data between sessions and includes date format with date-fns.\",\n  \"files\": {\n    \"/App.js\": {\n      \"code\": \"import React, { useState, useEffect } from 'react';\\nimport TodoList from './components/TodoList';\\nimport TodoForm from './components/TodoForm';\\nimport FilterBar from './components/FilterBar';\\nimport { Plus, Check } from 'lucide-react';\\n\\nfunction App() {\\n  const [todos, setTodos] = useState(() => {\\n    const savedTodos = localStorage.getItem('todos');\\n    return savedTodos ? JSON.parse(savedTodos) : [];\\n  });\\n  const [filter, setFilter] = useState('all');\\n\\n  useEffect(() => {\\n    localStorage.setItem('todos', JSON.stringify(todos));\\n  }, [todos]);\\n\\n  const addTodo = (text) => {\\n    if (text) {\\n     setTodos([...todos, { id: Date.now(), text, completed: false, createdAt: new Date() }]);\\n    }\\n  };\\n\\n  const toggleComplete = (id) => {\\n    setTodos(todos.map(todo => todo.id === id ? {...todo, completed: !todo.completed} : todo));\\n  };\\n\\n  const deleteTodo = (id) => {\\n    setTodos(todos.filter(todo => todo.id !== id));\\n  };\\n\\n  const filteredTodos = () => {\\n    switch (filter) {\\n      case 'completed':\\n        return todos.filter(todo => todo.completed);\\n      case 'incomplete':\\n        return todos.filter(todo => !todo.completed);\\n      default:\\n        return todos;\\n    }\\n  };\\n\\n  return (\\n    <div className=\\\"min-h-screen bg-gray-100 flex flex-col items-center p-4\\\">\\n       <h1 className=\\\"text-4xl font-bold text-center mb-8 text-blue-700\\\">My Todo List <Check className=\\\"inline-block ml-2 text-green-500\\\" /></h1>\\n      <div className=\\\"bg-white p-6 rounded-lg shadow-md w-full max-w-md space-y-4\\\">\\n       <TodoForm onAddTodo={addTodo} />\\n        <FilterBar onFilterChange={setFilter} filter={filter} />\\n        <TodoList todos={filteredTodos()} onToggleComplete={toggleComplete} onDeleteTodo={deleteTodo} />\\n      </div>\\n        <div className=\\\"mt-4 text-center text-gray-600\\\">\\n        <p>Crafted with ❤️ using React and Tailwind CSS. <Plus className=\\\"inline-block ml-1\\\"/> </p>\\n         </div>\\n    </div>\\n  );\\n}\\n\\nexport default App;\\n\"\n    },\n     \"/components/TodoList.js\": {\n      \"code\": \"import React from 'react';\\nimport TodoItem from './TodoItem';\\n\\nconst TodoList = ({ todos, onToggleComplete, onDeleteTodo }) => {\\n  return (\\n    <ul className=\\\"divide-y divide-gray-200\\\">\\n      {todos.length === 0 ? <li className=\\\"py-2 text-gray-500 text-center\\\">No todos yet...</li> : todos.map(todo => (\\n        <TodoItem key={todo.id} todo={todo} onToggleComplete={onToggleComplete} onDeleteTodo={onDeleteTodo} />\\n      ))}\\n    </ul>\\n  );\\n};\\n\\nexport default TodoList;\\n\"\n    },\n    \"/components/TodoItem.js\": {\n      \"code\": \"import React from 'react';\\nimport { Trash, Check, Clock } from 'lucide-react';\\nimport { format } from 'date-fns';\\n\\nconst TodoItem = ({ todo, onToggleComplete, onDeleteTodo }) => {\\n  const formattedDate = format(todo.createdAt, 'MMM dd, yyyy HH:mm');\\n  return (\\n    <li className=\\\"py-2 flex items-center justify-between\\\">\\n      <div className=\\\"flex items-center\\\">\\n          <input\\n            type=\\\"checkbox\\\"\\n            checked={todo.completed}\\n            onChange={() => onToggleComplete(todo.id)}\\n            className=\\\"mr-2 h-4 w-4 text-blue-600 focus:ring-blue-500 border-gray-300 rounded\\\"\\n           />\\n        <span className={`${todo.completed ? 'line-through text-gray-500' : 'text-gray-700'} text-sm`}>\\n         {todo.text}\\n        </span>\\n      </div>\\n      <div className=\\\"flex items-center space-x-2\\\">\\n         <span className=\\\"text-gray-400 text-xs\\\"> <Clock className=\\\"inline-block mr-1 h-3 w-3\\\" /> {formattedDate}</span>\\n        <button onClick={() => onDeleteTodo(todo.id)} className=\\\"text-red-500 hover:text-red-700\\\">\\n         <Trash className=\\\"h-4 w-4\\\"/>\\n        </button>\\n      </div>\\n    </li>\\n  );\\n};\\n\\nexport default TodoItem;\\n\"\n    },\n      \"/components/TodoForm.js\": {\n      \"code\": \"import React, { useState } from 'react';\\nimport { Plus } from 'lucide-react';\\n\\nconst TodoForm = ({ onAddTodo }) => {\\n  const [text, setText] = useState('');\\n\\n  const handleSubmit = (e) => {\\n    e.preventDefault();\\n    onAddTodo(text);\\n    setText('');\\n  };\\n\\n  return (\\n    <form onSubmit={handleSubmit} className=\\\"flex space-x-2\\\">\\n      <input\\n        type=\\\"text\\\"\\n        value={text}\\n        onChange={(e) => setText(e.target.value)}\\n        placeholder=\\\"Add a new todo...\\\" \\n        className=\\\"flex-grow px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500\\\"\\n      />\\n      <button type=\\\"submit\\\" className=\\\"bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-md\\\">\\n       <Plus className=\\\"inline-block h-4 w-4 mr-1\\\"/> Add\\n      </button>\\n    </form>\\n  );\\n};\\n\\nexport default TodoForm;\\n\"\n    },\n      \"/components/FilterBar.js\": {\n      \"code\": \"import React from 'react';\\n\\nconst FilterBar = ({ onFilterChange, filter }) => {\\n    return (\\n      <div className=\\\"flex justify-end mt-2\\\">\\n        <button\\n          onClick={() => onFilterChange('all')}\\n          className={`px-3 py-1 rounded-md ${filter === 'all' ? 'bg-blue-200 text-blue-800' : 'hover:bg-gray-100 text-gray-700'} `}\\n        >All\\n        </button>\\n        <button\\n          onClick={() => onFilterChange('completed')}\\n           className={`px-3 py-1 rounded-md ml-2 ${filter === 'completed' ? 'bg-green-200 text-green-800' : 'hover:bg-gray-100 text-gray-700'}`}\\n        >Completed\\n        </button>\\n         <button\\n          onClick={() => onFilterChange('incomplete')}\\n           className={`px-3 py-1 rounded-md ml-2 ${filter === 'incomplete' ? 'bg-yellow-200 text-yellow-800' : 'hover:bg-gray-100 text-gray-700'}`}\\n        >Incomplete\\n        </button>\\n      </div>\\n    );\\n  };\\n\\nexport default FilterBar;\\n\"\n    }\n  },\n  \"generatedFiles\": [\n    \"/App.js\",\n    \"/components/TodoList.js\",\n    \"/components/TodoItem.js\",\n    \"/components/TodoForm.js\",\n     \"/components/FilterBar.js\"\n  ]\n}\n```"},
      ],
    },
  ],
})

//   const result = await chatSession.sendMessage('INSERT_INPUT_HERE');
//   console.log(result.response.text());
